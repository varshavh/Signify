# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js

# python
.venv/
.venv-train/
__pycache__/
*.pyc

# app runtime output
saved_sentences.txt
data/app_state.json
data/*.tmp
data/*.db

# regenerable via build_combined_dataset.py (kept out of git to avoid bloat)
combined_dataset/

# testing
/coverage

# production
/build

# misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

# MediaPipe Model Maker training artifacts (only the .task is needed)
models/epoch_models/
models/logs/
models/best_model_weights.*
models/checkpoint
